{
  "name": "@jupiterone/integration-sdk-runtime",
  "version": "0.1.0",
  "description": "The SDK for developing JupiterOne integrations",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "repository": "git@github.com:JupiterOne/integration-sdk.git",
  "author": "JupiterOne <dev@jupiterone.io>",
  "license": "MPL-2.0",
  "engines": {
    "node": "10.x || 12.x || 14.x"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "jest",
    "compile": "tsc -p tsconfig.dist.json --declaration",
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "yarn compile && cp -R README.md ../../LICENSE package.json ./dist"
  },
  "dependencies": {
    "@jupiterone/integration-sdk-core": "^0.1.0"
  },
  "devDependencies": {
    "@jupiterone/integration-sdk-private-test-utils": "^0.1.0"
  }
}
